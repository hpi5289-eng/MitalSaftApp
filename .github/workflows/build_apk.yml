name: Build Mital Saft APKon: workflow_dispatchjobs:  build:    runs-on: ubuntu-latest    steps:      - uses: actions/checkout@v4      - name: Build APK        run: |          cd android          chmod +x gradlew          ./gradlew :app:assembleDebug      - name: Upload APK        uses: actions/upload-artifact@v4        with:          name: mital-saft-app          path: android/app/build/outputs/apk/debug/*.apk
